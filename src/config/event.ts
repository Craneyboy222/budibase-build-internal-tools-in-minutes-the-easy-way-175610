/* Event configuration */
export const EVENT_CONFIG = {
  logging: true, // Enable logging of events
  maxLogSize: 10485760, // 10MB
  logLevel: 'info', // Options: error, warn, info, debug
  notifyOnError: true, // Send notifications on errors
  notificationRecipients: ['devops@enterprise.com', 'admin@enterprise.com']
};